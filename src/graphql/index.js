// import { resolvers } from './resolvers'
// import { typeDefinitions } from './types'

// module.exports = { resolvers, typeDefinitions }

export { resolvers } from './resolvers'
export { typeDefinitions } from './types'
